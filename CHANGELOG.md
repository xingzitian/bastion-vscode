# 更新日志

本扩展遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## 0.2.0

一次以「体验」为主的更新：把几处**藏得太深**和**出错时说不清**的地方补上。

### 新增

- **用 rsync 增量同步到当前会话（🚧 实验性 · 未完成，默认关闭）**（资源管理器右键，文件或目录都行）：
  复用**当前那条已认证、
  已选完目标机的 shell 会话**，在里面注入 `rsync --server` —— 所以它穿得过堡垒机（SFTP 通常不行），
  而且**目标机不需要 lrzsz**、能整棵目录同步、传过的内容不重传。
  目标目录填 `.` 表示目标机当前目录。本机 rsync 用 `bastion.rsyncPath` 指定。
  这条路的两条关键细节是**实测**得来的（不是推测）：pty 上协议起点前有 10 字节终端噪声
  （`\x1b[?2004l\r\n`，bash 关括号粘贴时吐的）→ 必须**按握手形状找协议起点**；
  远端结束后必须把 EOF 传下去、让桥的 shim 退出 → 否则 rsync 和自己的子进程互等、永远不退出。
  两条都有单元测试守着。
  另外**目标目录不存在会自动建出来**（`mkdir -p`）；同步中文名/空格/引号/美元符文件名都试过；
  同一条会话同步完还能继续用；**中途取消后会话会在超时窗口内恢复**（不会卡死）。
  同步完成后会明确说清「传了几个文件」或「目标机已有相同文件、判定无需重传」，
  并说明**会话保持不动**（rsync 复用你当前这条会话，不会关掉它）。
  > **为什么标成「未完成」**：见下面「修复与改进」里的下线说明 —— 本地测试台全绿，
  > 但在某类堡垒机上大文件传到 ~95% 会停住，原因暂时定不到我们这侧。
  > **欢迎高手来查**，探针与测试台都已提交在仓库里。
- **rsync 同步现在有进度了**：状态栏与通知里实时显示「已发送 45 MB 49% · 922 KB/s · 已用 50s」，
  日志每 3 秒也记一条（含**两个方向**的字节数）。以前只有一个转圈 —— 用户实测就是因为看不到任何进度，
  以为卡死了，把一次 **91MB** 的传输取消了（其实它一直正常在传）。
  > 进度是自己算的：实测 rsync 的 `--info=progress2` 在 stdout 不是终端时**会被抑制**
  > （我们的桥就是把客户端 stdout 接成管道的），所以用「客户端已经发出去的字节数」当分子。
  > 目录同步时报「已发送 + 速率 + 已用」，不假装知道百分比（rsync 只传变化的部分，分母会偏大）。
- **卡住不再是死路**：连续 30 秒两个方向都没有字节流动会明确提示（已发送多少、还差多少），
  并给一个「**断开并续传**」按钮 —— 因为客户端一直带着 `--partial`，目标机上会保留已传的部分，
  续传时 rsync 只补差的那一段（E2E 里验证过：目标机已有 66% 时，实际只发不到 60% 的数据）。
  完成提示里还会写明「**本次实际发送 X MB**」—— 这样一眼就能看出 tar.gz（每次全量）和
  目录同步（只补差）的差别。
- **修掉一个让终端"回不来"的 bug**：注入远端的那行命令里为了不让提示符混进协议流写了 `PS1=`，
  而这是**持久改到你自己那条 shell** 的 —— 命令跑完后提示符成了空字符串，
  看起来像"终端没退回来"（敲命令其实有效，就是没有任何提示符）。现在改成**存下来再恢复**，
  并加了一条端到端断言（提示符必须回来）守着。
- **广播输入**：勾几台会话组成广播组，之后在**任意一个**会话里敲的命令会同时发给组里其它会话 ——
  同一个改动要在几台机器上各敲一遍时用。只同步人工键盘输入（MFA 动态码、AI 执行、部署注入都不广播），
  只读和已关闭的会话自动排除。**开着广播是危险的**（测试机上敲的 `rm` 会在生产机上跑），
  所以开着时底栏常驻一格警告色的「广播 N 台」、总览面板里的会话卡片挂「广播中」标记，
  点一下就能改或关；取消全部勾选 = 关闭。
- **粘贴 / 拖入本地文件路径就直接问「要不要上传」**：把 `C:\tmp\app.jar` 或 `/mnt/d/app.jar`
  粘进终端再回车，不再给远端发一条注定 `command not found` 的命令，而是弹一个「上传到当前会话」的确认。
- **从屏幕原文生成菜单规则**（`bastion.generateMenuRule`）：堡垒机菜单认不出来时，不用去日志里翻，
  直接选一句当时屏幕上的提示，扩展把它变成正则写进 `bastion.menuHints.*`，并给「重连试试 / 看看设置」两个按钮。
- **部署报告面板**：报告不再只是一份 Markdown 文件，而是右侧可交互面板 —— 按「跳过 → 失败 → 成功」排序，
  失败的一律默认展开，支持**单台重跑**、发给 AI、打开 Markdown。
  报告现在同时落一份 `.json`，**重启 VS Code 后还能打开上一次的报告**。
- **把最近一次部署报告发给 AI**（`bastion.sendLastReportToAI`）：报告可能几万字，送进 AI 前会截断并说明
  「省略了多少字符」，而不是悄悄截掉半句报错。
- **任务级「部署结束保留终端」**：`keepTerminal` 现在能写进单个任务，覆盖全局设置（只在这台机器上排障时用）。
- **底栏格子可以关**（`bastion.statusBarItems`）：底栏是稀缺资源，`session` / `readonly` / `overwrite` /
  `keepterm` / `broadcast` / `forward` 六个常驻格子能自己选显示哪几个。进度类格子（传输 / 部署 / MFA）
  **不在此列** —— 关掉它们只会让人不知道后台在干什么。
- **「快速上手」引导**（5 步 walkthrough）：从建档案到 AI 桥接，命令面板搜「BastionShell 快速上手」。
  每一步里的链接都能直接点开对应命令。

### 修复与改进

- **rsync 同步下线（🚧 标记为未完成）**：真机上大文件传到 ~95% 会停住（链路很快，是最后几 MB
  卡在管道里、远端也不再回数据；同一个文件用 rz 一直没问题）。本地测试台两协议全通过（含 512MB），
  所以这是**特定堡垒机环境**的问题，暂时定位不到我们这侧。要试的话打开设置 `bastion.rsyncEnabled`。
  > **求高手**：复现和二分定位用的探针都在仓库里 ——
  > [`tools/rsync-e2e/`](tools/rsync-e2e/)（本地测试台：真 SSH + 真 pty + 真 rsync 客户端）
  > 和 [bastion-shell/tools/rsync-probe](https://github.com/xingzitian/bastion-shell/tree/main/tools/rsync-probe)
  > （当时逐段二分用的探针 + `RESULTS.md` 记录）。有结论请开 issue，
  > 附上日志里每 3 秒一条的 `C2R/R2C` 字节数 —— 它正好能区分「客户端不再发」和「远端不再收」。
  > 另：已知 `tar.gz` 改一个字节就整包重排，rsync 也救不了；要增量请同步**目录**本身。
- **修掉一个会拖慢「冷启动」的问题**：恢复上次部署报告时，那份含每条命令输出的 `.json` 副本
  原来是**在 activate() 里同步读 + JSON.parse** 的（可能几 MB）—— 扩展激活正好发生在你点第一个
  命令时，于是第一个命令变慢。现在改成异步后台读。
- **冷启动现在可测量**：日志里新增两行 —— `扩展激活完成：X ms（同步部分）` 与
  `shell 已就绪（档案）｜冷启动 XXXX ms`。以后觉得慢，一眼就能看出是激活慢还是网络/MFA/菜单慢。

- **认证失败不再是一个「连接已断开」了事**：密码错 / 动态码错 / 主机指纹变了 / 超时 / 网络不通
  分开说清是哪一种，并给对应的下一步。MFA 动态码输错**不用从头重连**，直接在终端里重问一次
  （动态码 30 秒换一次，输错、输慢了是日常），最多三次。
- 认证失败导致的连接关闭不再弹「连接已断开，要重连吗」——那是失败不是掉线，重连也连不上。
- **主动取消连接不再被当成故障**：以前在弹出的输入框上点「取消」，会收到一个红色错误框
  「BastionShell：已取消连接」。现在只在终端留一行淡色提示 —— 取消是用户的意思，不是错误。
- **修掉状态栏悬停里的字面星号**：`保留终端` / `只读` / `同名:跳过` / `广播` 这几格的悬停说明写了
  markdown，但传的是纯字符串 —— VS Code 只对 `MarkdownString` 走 markdown 渲染，所以用户看到的是
  字面的 `**`。已统一包成 `MarkdownString`，并加了测试守住（新加格子时不会再犯）。
- **「粘/拖本地文件路径」原来基本不生效**：攒「当前这一行」的缓冲区只在带回车的那一块输入上更新，
  于是「粘一行 → 再单独按回车」（最常见的操作）永远攒不出内容，识别永远不触发。现在每一块输入
  都喂给攒行器，并把它抽成有测试的独立组件。
- 同上，**多行粘贴不再丢内容**：以前只要多行块的第一行恰好是本机文件路径，就会把后面几行整段吞掉
  （当成「要不要上传这个文件」处理）。现在多行块一律原样发给远端，不做路径识别。
- **广播输入有「原样同步」了**（默认，和桌面版一致）：你敲的每个键都镜像过去 ——
  **在 vim 里改配置文件、走交互式菜单必须用它**（那些不是「一行」，整行发送做不了）。
  另有「整行发送」模式：只在按回车时把整行发过去，敲到一半的内容、退格、方向键都不打扰对面。
  底栏广播开着时多出一格「原样同步 / 整行发送」，点一下即可切；设置项 `bastion.broadcastMode`。
- **广播可以直接在右侧「总览与进度」面板里逐台勾**（每台会话一个「加入广播 / 移出广播」按钮），
  不必每次都为改一台而过一遍多选列表。只读会话渲染成禁用的「不参与广播」并说明原因。
- **粘进来的本机路径不再先被远端当命令执行**：以前路径的字符在我们判断出来之前就已经发出去了，
  于是先看到 `-bash: c:/Users/.../DeepSeek: No such file or directory`，然后才问「要不要上传」——
  连 `rz -y` 都被接在同一行上执行。现在看起来像本机路径的输入会**先扣住不发**，
  早先已经发出去的字符用 Ctrl-U 擦掉，判断完再决定上传还是原样执行。
- **上传询问框被关掉时不再吞掉那一行**：以前直接关掉（Esc）等于把你敲/粘的那一行丢掉，
  看起来像终端吃了输入。现在关掉＝**把整行放回命令行、不执行**（想执行按回车，想去掉按 Ctrl-C）。
  点「当命令执行」则执行**整行**（以前只发路径，会把你后面敲的参数偷偷丢掉）。
- **MFA 重输改成「终端里提示 + 自动重问」**：以前失败会弹一个带按钮的警告框，
  必须点一下「重新输入动态码」才会重问 —— 动态码 30 秒就换，为点按钮把码等过期是真难受。
  现在只把说明写进终端，然后直接重问（最多 3 次，不想重试就关掉终端窗口）。
- **发给 AI 的截断现在也告诉用户**：报告太长时会被截断（默认 2 万字符），以前只有 AI 看到那句
  「后面还有 N 个字符没发」，用户以为整份都发出去了 —— 而恰好被切掉的可能是失败那台的输出。

## 0.1.5

**本版没有功能改动**，只补齐商店上 0.1.3 安装包缺失的发布元数据与文档：

- 清单补齐 `repository` / `bugs`（GitHub 仓库地址）与 `license: MIT`，并加入 `LICENSE` 文件
- README 顶部加动态徽标（CI 状态 / 许可证 / 最近提交）与仓库、商店、问题反馈入口
- 修复打包时 `vsce` 把会话编号 `#1`/`#2` 误当作 GitHub issue 引用的问题
  —— 它在代码块里，商店页上会原样显示成裸 markdown

> 功能与 0.1.3 相同（菜单「读屏幕」识别、上传后等提示符再发下一条命令、部署结束保留终端开关）。
> 如果你本地装的是更早的 0.1.3 包，重装本版即可拿到全部修复。

## 0.1.3

自首个公开版本以来的一次大更新。（`0.1.1` / `0.1.2` 是本地迭代，未上传商店。）

### 新增

- **「总览与进度」面板**（停靠在右侧辅助侧边栏）：点底栏任意 Bastion 格子即可展开。
  实时显示会话（编号/连接状态/已连时长/复用数）、传输进度条（速率 + 剩余时间）、
  部署进度条（可停止）、端口转发，并且每个会话都能「聚焦 / 设为只读 / 关闭」。
  底栏格子原来各自不同的点击行为统一成「打开这个面板」，原来那些动作全部搬进了面板。
- **部署任务行内 ▶ / ■ 按钮，以及「停止」能力**（以前跑起来就没法停）。停止粒度是「跑完当前这台就停」——
  中途硬中断会把目标机留在半执行状态，比多等一台危险。剩余未跑的会在报告里标成「未执行」。运行中的任务图标会转圈并显示 `3/8`。
- **部署报告改为记录每条命令的实际输出**（以前只有一张没用的耗时表）。`preCommand` / `script` 现在**逐行执行**，
  所以「哪条命令产生了什么输出」能对应上；交互式命令会明确标注输出无法捕获并指路怎么去看现场。
- **直连档案跳过选机/选用户两步**：直连的目标机就是档案自己那台，`hosts` 不用填，执行时不再走堡垒机菜单，
  批量连接也直接开会话。
- **个人习惯（`~/.bastionshell/habits.jsonc`）**：把「你在这台机器上怎么干活」记成文件（提权方式 / 常用目录 / 口头习惯）。
  新增 `bastion_habits` 工具（`read` / `remember` / `setPrivilege`），AI 连接后先读它，不再每次都问「要不要 `sudo -i`」。
  写入只改目标字段，手写的中文注释不会被冲掉。
- **终端内容一键发给 AI**：终端里选中文字后按 `Ctrl+Alt+I` 送进 AI 聊天（和编辑器里选代码发给 AI 一个手感）；
  没选中时按 `Ctrl+Alt+Shift+I` 发最近若干行输出。两个命令也在终端右键菜单里。
- **状态栏分槽位**：常驻（会话 / 只读 / 上传覆盖方式）+ 临时（传输进度 / 部署 / 转发 / MFA），按需出现、结束即消失。
- **传输进度与历史**：底栏实时显示百分比、平均速率、剩余时间；传完写进「传输历史」视图，**失败的上传可右键重试**。
- **上传覆盖方式可配**：`bastion.uploadOverwrite` = `skip`（默认）/ `overwrite` / `rename`，也可点状态栏切换。
- **只读模式**：一键把会话切成「只看不敲」（生产防手滑），三个入口：状态栏 / 命令面板 / 终端右键。
- **会话号与连接状态**：终端名带 `#1`、`#2` 编号；状态栏悬停可看每个会话的可写/只读、已连时长，以及底层连接的认证状态与复用会话数。
- **部署进度与报告**：底栏显示 `部署 3/8`，跑完生成 Markdown 报告（逐台成功/失败/耗时/失败原因 + 任务配置回放）。
- **端口转发行内按钮**：每条规则右侧 ▶ / ■ 单独启停；「全部停止」移到视图标题栏并加确认弹窗。
- **快捷命令支持长命令**：`command` 可以是数组（一行一个），也可以用 `file` 指向外部 `.sh` 文件；新增 `description` 和 `sendEnter`。
- **部署任务带字段提示**：新增 JSON Schema，编辑任务时悬停字段名即显示中文说明、自动补全、写错字段名会报错；任务文件改为 `.jsonc`，可自由写注释。
- **搜索关键词**：补了 22 个关键词（`ssh`、`terminal`、`bastion`、`堡垒机`、`跳板机` 等）—— 之前商店里基本搜不到本扩展。
- **商店图标**（128×128）、`CHANGELOG.md`、`bastion.showLog` 命令（一键打开日志通道）。
- **常驻单元测试**：231 项，`npm test` 一条命令跑完，不需要启动 VS Code（含用假终端模拟堡垒机画屏）。

### 修复

- **「部署结束保留终端」有开关了**（以前只有设置项 `bastion.deployTerminalPolicy`，用户找不到）。
  底栏常驻一格「保留终端」（开着才显示，点一下关掉）、命令面板搜「部署结束保留终端窗口」、
  「总览与进度」面板的快捷操作里显示开/关、「部署任务」视图标题栏也有图标。
  排查问题最需要的往往就是那个窗口里的原始输出，所以它必须看得见、点得到。
- **菜单识别彻底改成「认屏幕」而不是「在数据流里找提示」。**
  这一步反复失败过好几轮，根因有两个，都跟「在原始数据流里匹配」有关：
  1. `emitOutput` 曾经每收到一个数据块就单独剥离 ANSI，而 `\x1b[32m` 这类转义序列经常被 TCP 拆到两个块里，
     逐块剥离会留下残缺的 `\x1b[3` 和 `2m`，拼起来文字变成 `输\x1b[32m入` —— 规则再也匹配不上。
     症状极具迷惑性：**日志里 dump 出来的屏幕原文是干净的**（dump 走的是整段剥离），看着完全正常，就是认不出来。
  2. 堡垒机画菜单是「一屏一屏刷」，夹着 `\r` 回行覆盖和光标移动，同一个位置在数据流里叠了好几遍，
     跟屏幕上真正显示的不是一回事；而且常常**分几段画**（先表头、再表格、再提示），
     只等一次静止就会在「画了一半」时被判成没认出来 —— 于是「菜单明明很快就出来了，就是没捕获到」。

  现在的新做法：缓冲里存**原始数据** → 每次操作**先打屏幕标记** → 等这一屏画完（输出静止 0.7~0.9 秒）→
  读**标记之后**的整段画面（去 ANSI + `\r` 只留最后一段）→ 用正则匹配；分几段画就多等几轮（最多 4 轮，总预算 20 秒）。
  两个细节很关键，都有回归测试钉着：
  - 标记若正好落在一个转义序列中间，**退回到那个 `\x1b` 再读**，否则读到的开头会粘着 `2m`，
    把 `^\s*opt>`、`^\s*id>` 这类**行首锚定**的规则顶掉（`safeReadStart`）。
  - 只看标记之后的内容，否则滚动缓冲里那份**旧主菜单**会让「IP 没被接受」误判成真。
- **上传之后的命令偶尔根本没被执行（报告里看起来像「脚本没输出」）。**
  这个是**掷硬币**，不是必然 —— 同一台机器 10 次里 5 次丢：

  | 上传 | 紧接着的 `cat README.md` |
  |---|---|
  | 成功（0.3s） | 0.1s，捕获到全文 ✅ |
  | 成功（0.5s） | 3.1s，只有一个提示符 ❌ |
  | **被对端跳过** | 3.0s，只有一个提示符 ❌ |

  3.0~3.1 秒正好是「输出静止 3 秒」兜底，说明结束标记**从未上过线** ——
  而捕获输出直接挂在 SSH 原始数据流上（不经过 zmodem 那一层），所以这**不是「日志没打印」**。
  真因：zmodem 协议结束（本地 `close()` 返回）≠ 远端 `rz` 已退出，
  这段窗口里发过去的命令被 `rz` 当成协议数据吃掉。
  修法就一件事：**上传结束后等远端提示符回来，再交还给调用方**（等不到也只是慢一点，不报错）。
- **报告不再让「没等到结束标记」看起来像「跑了但没输出」**：新增一行警告，说明命令
  **很可能没有真正执行**。这条以前是隐形故障，连续多次部署报告都只有空输出，
  方向全被带偏到「脚本是不是有问题」。
- **配置写入的 rename 加了退避重试**：Windows 上目标文件被杀毒/索引/编辑器短暂占用时
  `MoveFileEx` 会抛 EPERM/EBUSY，表现为「改了设置却没保存」且毫无提示。
- **「没认出来」时给出判断**：区分「屏幕上有编号列表 / `xxx>` 提示符 → 是菜单，只是规则没覆盖」
  和「屏幕上没有菜单特征 → 很可能压根没有这一步，留空 userChoice 更省事」。
  以前只丢一段原文，用户不知道该改配置还是该留空。
- **主菜单的强/弱特征拆成两个设置**：裸提示符（`Opt>`、`[Host]>`）单独放 `bastion.menuHints.hostPromptLoose`，
  只用于「第一次等主菜单」。判断「IP 被拒绝、又回到输 IP」**只用强特征** ——
  因为输完 IP 后那行回显是 `Opt> Opt> 10.0.0.10`，用提示符判断会把它误当成「又回到输 IP」而白白报错。
- **部署报告有了稳定入口**：报告原来只在结束时弹一下状态栏就没了，等你想看已经找不到。
  现在视图标题栏有 📄 按钮（`bastion.openLastDeployReport`）、总览面板里有「最近一次部署」一节、日志里也写出报告路径。
- **单击语义统一**：每个列表项的**单击 = 该项最自然的动作**，其它动作一律右键菜单（分组为
  `1_主操作` / `2_编辑|2_操作` / `3_删除`）。唯一例外是**部署任务：单击 = 打开文件编辑**，
  执行走行内 ▶ 按钮 —— 跑一次部署不能是「手滑点一下」的后果。
- **部署任务的命令字段支持数组写法**（`"preCommand": ["sudo -i", "cd /etc"]`）—— 和快捷命令、docker compose
  一个手感，不用再写 `\n` 转义。字符串写法仍然兼容，两种可混用。以前只支持字符串，
  用户「想输多条语句却不知道怎么写」是真实反馈。
- **覆盖模式的报错不再骗人**：以前那条「远端已存在 X（覆盖模式=跳过），未上传」是**写死的**，
  于是设了「覆盖」却仍被跳过时，日志把原因说成是跳过模式造成的，排错方向直接跑偏。
  现在会分清「按 skip 设置跳过」和「已发出覆盖请求但对端拒绝了」，并把**实际发出的 rz 命令**打进日志
  （`启动上传：\`rz -y\`（覆盖模式=overwrite，来自设置 bastion.uploadOverwrite）`）。
- **报告如实写出实际覆盖模式**，并说明未上传的真实原因。
- **菜单导航日志补上耗时**：`命中「userPrompt」（900ms）`。
- **直连档案执行部署时白等菜单、还把 host 打进 shell**：直连也会走堡垒机那套「等选机菜单 → 输 IP → 选用户」，
  于是干等一个永远不出现的菜单（6 秒超时 + 1.2 秒兜底），再把档案里的 `host` 当成目标机 IP 打进已经进去的 shell。
- **`hosts` 为空时部署任务静默什么都不做**：报告显示 `0/0 成功`，看起来像「跑了但没反应」。
  现在会直接报错说明原因（堡垒机档案必须在 `hosts` 里填目标机）。
- **管理员账号没有「选用户」这一步时会把序号打进 shell**：有些堡垒机账号输完目标机 IP 直接进 shell。
  旧实现会傻等选用户菜单超时，然后把序号（默认 `1`）打进已经进去的 shell —— 既白等 10 秒，又执行了一条叫 `1` 的命令，
  还让后续命令全部错位。现在输完 IP 会同时等「选用户菜单 / shell 提示符 / 又回到输 IP」，按先到的分支走。
- **目标机 IP 没被接受时静默错位**：堡垒机会把菜单重画回输 IP 那一步，现在能识别并**直接报错说明原因**，不再硬着头皮往下发。
- **连接断开后端口转发不清理**：`stopForwardsOfConn` 导入了却从未被调用，死隧道会一直留在列表里。
- **一条写错的条目会让整份配置被清空**：快捷命令、转发规则的校验原本要求「数组里每一项都合法」，
  只要一条字段写错，整份文件就被判为损坏、备份成 `.bak` 并清空列表。现在改为逐条筛选，坏条只跳过自己。
- **高危命令黑名单误报**：旧规则会把 `rm -rf /tmp/build` 判成「删除根目录」。误报会让人养成无脑点确认的习惯，
  防线等于没有。规则改为锚定「几乎不可能是本意」的写法（`rm -rf /` 报，`rm -rf /tmp/x` 不报；备份方向的 `dd if=/dev/sda of=…` 不报）。
- **小文件传完记成 `0 B/s`**：样本太短算不出速率时不写这个字段，而不是存 0。
- **命令里混进裸 `\r` 会提前提交半条命令**：换行拆分现在同时认 `\n` / `\r\n` / 裸 `\r`。
- **菜单认不出来时不再让人干瞪眼**：以前只打印一句「未识别到提示」，用户既看不到自己的菜单长什么样、
  也没法把规则填进设置，等于死路。现在会把**当时屏幕上的原文整段打出来**，并指明该改哪个设置。
- **`[zmodem] detect frame hex` 刷屏**：收进 `bastion.verboseLog`（默认关），不再把真正有用的日志行挤掉。
- **清单漂移**：`bastion_exec` 等 4 个语言模型工具漏了 `onLanguageModelTool:` 激活事件（只有后加的 `bastion_habits` 有）。
  新增的清单对账测试当场抓到的。

### 改进

- **堡垒机菜单导航不再靠固定等待**：原来是一串 `sleep(800/1500/2000)`，网络慢一拍就错位。
  现在等屏幕画完再读屏认提示，认出就立刻走（主菜单实测 ~400ms 命中）；
  认不出来退回固定等待（等于旧行为，不会更差）。识别正则可用 `bastion.menuHints.*` 覆盖，不用等作者改代码。
  内置规则全部来自**真实抓下来的菜单原文**（某厂商堡垒机 实测：`进行搜索` / `输入资产[...]的账号ID` / `ID>`），
  不是猜的；英文/日文界面也有对应写法。
- **高危命令检查扩展到部署任务与快捷命令**（原本只拦 AI），人工路径用「确认」而非「拒绝」——命令是你写的，你有权执行。
- **部署结束后回收成功会话的终端**：失败的**保留**（那里是出错现场，关掉等于销毁证据），策略由 `bastion.deployTerminalPolicy` 控制。
- **任务结束时不再弹窗打断**：完成回执走状态栏，几秒后自动消失。
- **改了配置文件保存即生效**：监听 `~/.bastionshell/*.jsonc`，侧边栏自动刷新，不用手动点。
- **AI 收到的高危拦截信息带原因**：说明命中了哪条规则、匹配到哪段原文，而不是一句「已被拦截」。

## 0.1.0

首个公开版本。

- 连接复用：一条堡垒机连接认证一次（密码 + MFA），后续开 N 个 shell 会话直接复用。
- MFA 在终端内输入（带倒计时），不弹窗。
- rz/sz 文件传输（ZMODEM），适配 lrzsz。
- 部署任务：文件式任务，批量连接目标机 → 前置命令 → 上传 → 脚本。
- 快捷命令、端口转发、批量连接。
- AI 桥接：`bastion_exec` 让运行在 VS Code 里的 AI 插件能在服务器上执行命令。
